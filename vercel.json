{
    "version": 2,

    "routes": [
        {
            "src": "^/users/(.*)",
            "dest": "server/server.js"
        },
        {
            "src": "^/(.*)",
            "dest": "client/public/index.html"
        }
    ]
}